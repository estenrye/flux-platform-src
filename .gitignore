.DS_Store
.mcp.json# controlplane cluster: private age key stays out of git (1Password holds it)
clusters/controlplane/.sops.age-key
# crossplane private age key: untracked (was committed since afe949f — rotate the
# crossplane age key, its exposure is already in git history)
clusters/crossplane/.sops.age-key
# blanket rule for every other cluster's private age key (1Password holds
# the recovery copy) — bootstrap-cluster-sops-key.sh writes this file but
# doesn't itself gitignore it per-cluster, so this must stay generic
clusters/*/.sops.age-key
# tofu working files: state stays on the workstation, media cache is derived
providers/kvm/.cache/
providers/kvm/**/.terraform/
providers/kvm/**/.terraform.lock.hcl
providers/kvm/**/*.tfstate*
# rendered Talos machine configs contain live secrets
providers/kvm/controlplane/.rendered/
etcd-backups/
