# Claude Code Project Config

## Memory

Project memory lives at `docs/memory/` in this repo (not the default `~/.claude` path), so it is tracked with the project.

Index: [docs/memory/MEMORY.md](docs/memory/MEMORY.md)

## Skills

Project skills live at `.claude/skills/` in this repo (not the default `~/.claude/skills/` path), so they are tracked with the project.
