apiVersion: chainsaw.kyverno.io/v1alpha2
kind: Configuration
metadata:
  name: controlplane-baseline
spec:
  timeouts:
    apply: 60s
    assert: 180s
    cleanup: 180s
    delete: 180s
    error: 180s
    exec: 300s
