apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: github-token
  namespace: crossplane-system
spec:
  secretStoreRef:
    name: 1password-sdk
    kind: ClusterSecretStore
  refreshPolicy: OnChange
  target:
    name: github-token
    creationPolicy: Owner
    deletionPolicy: Delete
    template:
      metadata:
        labels:
          app: crossplane-provider-github
        annotations:
          description: "GitHub App credentials for Crossplane Provider GitHub, managed by External Secrets Operator"
      data:
        credentials: |
          {"app_auth":[{"id":"{{ .app_id }}","installation_id":"{{ .installation_id }}","pem_file":"{{ .pem_file | replace "\n" "\\n" }}"}],"owner":"{{ .owner }}"}
  data:
    - secretKey: app_id
      remoteRef:
        key: github-auth-app/app_id
    - secretKey: installation_id
      remoteRef:
        key: github-auth-app/installation_id
    - secretKey: pem_file
      remoteRef:
        key: github-auth-app/private-key
    - secretKey: owner
      remoteRef:
        key: github-auth-app/owner
