[defaults]
# Auto-discovered when ansible-playbook is run from this directory (see
# docs/runbooks/kvm-host-maintenance.md) -- lets playbooks/ find roles/ as
# siblings without an absolute path.
roles_path = ./roles
inventory = ./inventory/kvm-hosts.yml
host_key_checking = True
