apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: "envoy-gateway-gateway-helm-certgen:envoy-gateway-system"
  annotations:
    checkov.io/skip1: "CKV_K8S_155=Envoy Gateway certgen job requires ValidatingWebhookConfiguration and MutatingWebhookConfiguration access to configure admission webhooks for the Gateway API controller."
