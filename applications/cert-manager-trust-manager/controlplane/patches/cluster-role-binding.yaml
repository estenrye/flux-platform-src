- op: add
  path: /metadata/annotations/ignore-check.kube-linter.io~1access-to-secrets
  value: "M4 completion step B6: secretTargets.enabled=true (values.yaml) is required so a Bundle can target a Secret (openbao-server-8443-fullchain). The chart grants get/list/watch on ALL secrets cluster-wide for this feature, regardless of authorizedSecrets write-scoping -- a real, accepted, documented tradeoff (see values.yaml's own comment), not an oversight."
