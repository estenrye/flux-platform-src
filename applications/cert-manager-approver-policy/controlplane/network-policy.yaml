apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cert-manager-policy-approver-allow-egress
  namespace: cert-manager
spec:
  podSelector:
    matchLabels:
      app: cert-manager-approver-policy
  policyTypes:
  - Egress
  egress:
  # apiserver targetPort - Calico evaluates egress post-DNAT
  # (docs/memory/calico-networkpolicy-dnat.md). Talos serves the apiserver
  # on 6443; the namespaceSelector rule below cannot match node endpoints.
  - ports:
    - protocol: TCP
      port: 6443
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: default
    ports:
    - protocol: TCP
      port: 443
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: kube-system
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cert-manager-policy-approver-allow-ingress
  namespace: cert-manager
spec:
  ingress:
  - ports:
    - port: webhook
      protocol: TCP
    - port: metrics
      protocol: TCP
    - port: healthcheck
      protocol: TCP
  podSelector:
    matchLabels:
      app: cert-manager-approver-policy
      app.kubernetes.io/instance: cert-manager-approver-policy
      app.kubernetes.io/name: cert-manager-approver-policy
  policyTypes:
  - Ingress