apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: cnpg-cloudnative-pg
  annotations:
    checkov.io/skip1: "CKV_K8S_155=CNPG operator requires ValidatingWebhookConfiguration and MutatingWebhookConfiguration access to install and manage its own admission webhooks."
