apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: openbao-db-barman
  namespace: openbao
spec:
  configuration:
    destinationPath: s3://openbao-db-barman/
    endpointURL: http://garage.garage.svc.cluster.local:3900
    s3Credentials:
      accessKeyId:
        name: openbao-db-barman-credentials
        key: access-key-id
      secretAccessKey:
        name: openbao-db-barman-credentials
        key: secret-access-key
    wal:
      compression: gzip
    data:
      compression: gzip
  retentionPolicy: "30d"
