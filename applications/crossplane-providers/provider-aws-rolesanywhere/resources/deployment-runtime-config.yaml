apiVersion: pkg.crossplane.io/v1beta1
kind: DeploymentRuntimeConfig
metadata:
  name: aws-rolesanywhere-admin-provider
  namespace: crossplane-system
spec:
  deploymentTemplate:
    spec:
      selector:
        matchLabels:
          app: upbound-provider-aws-rolesanywhere
      template:
        metadata:
          labels:
            app: upbound-provider-aws-rolesanywhere
        spec:
          securityContext:
            runAsGroup: 2000
            runAsNonRoot: true
            runAsUser: 2000
            fsGroup: 2000
          serviceAccountName: aws-rolesanywhere-admin-provider
          containers:
            - name: package-runtime
              env:
                - name: AWS_REGION
                  value: us-east-2
                - name: AWS_EC2_METADATA_SERVICE_ENDPOINT
                  value: http://localhost:9911
            - name: aws-signing-helper
              image: public.ecr.aws/rolesanywhere/credential-helper:1.8.1-2026.04.09.16.01
              command:
                - aws_signing_helper
                - serve
                - --port
                - "9911"
                - --debug
                - --certificate
                - /var/run/secrets/spiffe.io/tls.crt
                - --private-key
                - /var/run/secrets/spiffe.io/tls.key
                - --trust-anchor-arn
                - arn:aws:rolesanywhere:us-east-2:832767337984:trust-anchor/1433b5ab-1a7a-4134-9d84-baa79f94d093
                - --profile-arn
                # Bootstrap required: create an IAM Roles Anywhere profile that references
                # the crossplane-provider-rolesanywhere-admin role, then replace the placeholder below.
                - arn:aws:rolesanywhere:us-east-2:832767337984:profile/14d6a23d-cc8b-480c-8ae2-5d006359bf25
                - --role-arn
                # Bootstrap required: create an IAM role named crossplane-provider-rolesanywhere-admin
                # with permissions to create and manage Roles Anywhere profiles,
                # then replace the placeholder below.
                - arn:aws:iam::832767337984:role/crossplane-provider-rolesanywhere-admin
              imagePullPolicy: IfNotPresent
              ports:
                - containerPort: 9911
                  name: imdsv2
                  protocol: TCP
              volumeMounts:
                - mountPath: "/var/run/secrets/spiffe.io"
                  name: spiffe
          volumes:
            - name: spiffe
              csi:
                driver: spiffe.csi.cert-manager.io
                readOnly: true
