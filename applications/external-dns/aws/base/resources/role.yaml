apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: external-dns-allow-certificate-requests
  namespace: external-dns
rules:
- apiGroups: ["cert-manager.io"]
  resources: ["certificaterequests"]
  verbs: ["create"]