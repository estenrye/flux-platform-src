apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: keycloak-db-controlplane
  description: |-
    CNPG PostgreSQL cluster for Keycloak on the controlplane cluster,
    with barman continuous WAL archiving + base backups to Garage via
    the shared cnpg-barman-plugin (M3 step 8).
  tags:
    - database
    - platform
    - infrastructure
spec:
  type: service
  lifecycle: production
  owner: platform-engineering
